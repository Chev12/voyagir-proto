{% extends "base.html.twig" %}
{% block body %}
    <div style="width: 60%">
        <a href="{{ path("admin_home") }}">Accueil admin</a>
        <h2> Informations générales </h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}